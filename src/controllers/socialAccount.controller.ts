import prisma from "../prismaClient/client.js";
import asyncHandler from "../utils/asyncHandler.js";

export const createSocialAccount = asyncHandler(
  async (req: Request, res: Response) => {
    
  }
);
